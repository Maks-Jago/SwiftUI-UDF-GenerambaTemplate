{% include 'header' %}

import SwiftUI
import SwiftUI_UDF

struct {{ module_info.name }}Routing: Routing {
    enum Route {
        //case some
    }
    
    @ViewBuilder func view(for route: Route) -> some View {
        switch route {
        //case .some: Text("Some")
        }
    }
}
