{% include 'header' %}

import SwiftUI
import SwiftUI_UDF

struct {{ module_info.name }}Component: Component {
    struct Props {

    }
    
    var props: Props
    
    var body: some View {
        Text("{{ module_info.name }}")
    }
}

struct {{ module_info.name }}Component_Previews: PreviewProvider {
    static var previews: some View {
        {{ module_info.name }}Component(props:
            .init(
                
            )
        )
    }
}